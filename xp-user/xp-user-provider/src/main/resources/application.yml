spring:
  application:
    name: xp-user-provider
  datasource:
    url: jdbc:mysql://tx-001:3306/x-peas?useUnicode=true&characterEncoding=utf-8&useSSL=false
    username: root
    password: Lijinquan9900
    driver-class-name: com.mysql.jdbc.Driver
  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        hbm2ddl.auto: update

eureka:
  client:
    service-url:
       defaultZone: http://localhost:8761/eureka/
  instance:
    instance-id: xp-user-provider
    prefer-ip-address: true

server:
  port: 0
info:
  application.name: xp-user-provider
  version: 1.0-SNAPSHOT
  coder:
    name: Lijq
    email: ly_0199@163.com


